[project]
name = "quant-arb-engine"
version = "0.6.0"
description = "Multi-strategy arbitrage research engine (paper/research only) — sibling of funding-arb (locked measurement baseline)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = []

[tool.setuptools]
packages = ["quant_arb", "quant_arb.models", "quant_arb.edge", "quant_arb.feeds",
            "quant_arb.strategies", "quant_arb.risk", "quant_arb.research",
            "quant_arb.rfq", "quant_arb.rfq.providers"]

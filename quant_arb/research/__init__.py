"""Research layer — derived statistics over research journals.

Pure stdlib. Everything produced here is a MACHINERY DIAGNOSTIC on a
synthetic world: it validates code paths and estimator behavior on
deterministic mock data, and is never evidence about any market.

Artifacts written by the sweep (run-latest.json / sweep-latest.json) are
plain derived-summary files, NOT journal-managed records — the append-only,
invariant-enforced journals remain the source of truth.
"""
